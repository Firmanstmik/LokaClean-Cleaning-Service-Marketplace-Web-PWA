# Server
NODE_ENV=development
PORT=4000

# CORS (frontend dev origin)
CORS_ORIGIN=http://localhost:5173

# Public base URL (used to form absolute URLs if needed)
BASE_URL=http://localhost:4000

# Upload directory (local disk)
UPLOAD_DIR=uploads

# JWT
# IMPORTANT: Use a long random secret in production (>= 32 chars).
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_32_CHARS_MIN
JWT_EXPIRES_IN=7d

# Database
# Use your local or cloud PostgreSQL connection string.
# Example:
# postgresql://username:password@localhost:5432/lokaclean?schema=public
DATABASE_URL=postgresql://username:password@localhost:5432/lokaclean?schema=public

# Reverse geocoding (optional, used to show an approximate address under the map pin)
# By default we use the public OpenStreetMap Nominatim endpoint.
# For production workloads, consider a paid provider or your own Nominatim instance.
#NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org
#NOMINATIM_USER_AGENT=LokaClean (http://localhost:4000)
#NOMINATIM_EMAIL=dev@example.com

# Seed (local/dev convenience)
ADMIN_SEED_FULL_NAME=LokaClean Admin
ADMIN_SEED_EMAIL=admin@lokaclean.local
ADMIN_SEED_PASSWORD=admin12345

# Midtrans Payment Gateway (SANDBOX) - OPTIONAL
# Payment features will be disabled if not configured
# Get your credentials from: https://dashboard.sandbox.midtrans.com/
# Leave commented out or remove if you don't need payment features yet
# NOTE: Depending on when your Midtrans account was created, the sandbox key
# may look like either "SB-Mid-server-..." or "Mid-server-..." (and similarly
# for client key). Both formats are supported by this app.
#MIDTRANS_SERVER_KEY=Mid-server-xxxxxxxxxxxxxxxxxxxxx
#MIDTRANS_CLIENT_KEY=Mid-client-xxxxxxxxxxxxxxxxxxxxx
#MIDTRANS_IS_PRODUCTION=false

# Web Push (VAPID) - OPTIONAL but recommended for PWA notifications
# Generate with node:
#   const webpush = require('web-push'); 
#   const keys = webpush.generateVAPIDKeys(); console.log(keys)
#VAPID_PUBLIC_KEY=BNxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
#VAPID_PRIVATE_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
#VAPID_SUBJECT=mailto:admin@lokaclean.com


